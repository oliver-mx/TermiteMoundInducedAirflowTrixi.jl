name: Format-suggestions

on: [pull_request, workflow_dispatch]

permissions:
  actions: read
  contents: read

jobs:
  code-style:
    runs-on: ubuntu-latest
    steps:
      - uses: julia-actions/julia-format@v3
        with:
          version: '1.12.6'
          suggestion-label: 'format-suggestions'